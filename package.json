{
  "name": "voice-enabling-text-bot-application",
  "version": "1.0.0",
  "description": "This application allows placing or receiving voice calls to/from a phone and interact with a text-only chatbot via voice, thus voice enabling a text-only chatbot.",
  "main": "voice-on-text-bot-app-generic.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tony Chan",
  "license": "Apache-2.0",
  "dependencies": {
    "@vonage/server-sdk": "^3.16.0",
    "axios": "^1.7.2",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
